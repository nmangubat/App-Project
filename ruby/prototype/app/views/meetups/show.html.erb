<h1> <%= @meetup.festival %> </h1>

<p> Hey we're meeting during <%= @meetup.festival %> at <%= @meetup.location %> on <%= @meetup.time %> </p>

<%= link_to "Edit this meetup!", edit_meetup_path(@meetup) %>
<br>

<%= link_to "Delete this meetup!", meetup_path, method: :delete, data: {confirm: "you sure?"} %>

<br>

<h2>Comments</h2>
<% @meetup.comments.each do |comment| %>
  <p>
    <strong>Commenter:</strong>
    <%= comment.commenter %>
  </p>
 
  <p>
    <strong>Comment:</strong>
    <%= comment.body %>
  </p>
<% end %>
 

<h2>Add a comment:</h2>
<%= form_for([@meetup, @meetup.comments.build]) do |f| %>
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to "Back to the list!", meetups_path %>